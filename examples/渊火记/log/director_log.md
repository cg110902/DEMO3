# director_log — 渊火记

## 生产审计（M5 验收口径：每章 LLM 调用 ≤4）
| 章 | LLM 调用（岗位） | 引擎 CLI（确定性，不计入上限） |
|---|---|---|
| ch_001 | director(beats+任务书) / drafter(raw) / guard(final+注记) / syncer(提案) | gaps, pack, style, dup, sync×2（一次红→修→绿）, check |
| ch_002 | 同上 4 | pack, style, dup, sync, check |
| ch_003 | 同上 4 | style, dup, sync×2（status 枚举红→修→绿）, check |
| ch_004 | （beats 草稿已备，下一会话续） | pack 触发率演示 6/6 |

结论：单章 LLM 调用恒为 4；引擎调用全为无状态读或唯一写入口（sync）。

## 改稿教训（卷内回流）
- "灯油"两章各 1 次，自查定为 tic → 已入 style_guards（ch_004 起 pack 硬提醒携带；
  ch_001/002 各挂 style_guard_hit 警告留档——历史章不回改，留警告即账）。
- 人物卡"改口常翁"未在 ch_003 落地（审校注记⑤）：称谓进度条顺延 ch_006 堂前戏——
  beats-builder 的"新实体标记"要含"称谓变更"类承诺，否则 Stage 1 会漏账（下卷在任务书模板加一行自查）。
- entities.status 是枚举非自由文本，连踩两次（ch_001/ch_003）：属模板缺口——
  inbox/README 样例已含正确写法，但示例只演示了 item；下版样例补一条 place 的 upsert（工程待办，不改引擎）。

## 反思（十行内）
三章闭环证明流程可跑且引擎够薄：真正吃工的是 Stage 1 的任务书设计与 Stage 3 的重铸幅度，
两处都不可外包给校验。下一步 ch_004 起执行卷纲既定"主角会输"线；若 ch_006 前 GUN-001 出现
第二张牌未动，按 overdue 提醒在 ch_005 强制 remind 一次。

## 卷末反思（vol_01 收卷，十行内）
1. 闭环达成：6 章全绿、引擎调用 4/章、提案两次被拒（status 枚举）均在 failed/ 留痕——协议刚性实证。
2. **章长均匀病**：2221/2202/2203/2207/2206/2207，极差 19 字。craft 反均匀靠 Stage 1 的 words 带差异执行，
   本卷各章带太近（2200-3600/2400-3600/2600-4200/2200-3600/2600-4200/2400-3600）且我逐章贴着下沿写。
   卷二规则：相邻章带宽错开≥600 字，静章/短章配额写死进 beats front-matter 的 words。
3. 引擎盲区确认：word_band_deviation 只查全局带；跨章方差无检——不自建 checker（过度设计），以反思代检。
4. "数"字这类**条件禁忌**不入 style_guards（单字假阳性），留任务书逐章下——guard 清单只收"永远坏"多字短语。
5. 殿影四章不出场只留物证（条/印/箭/灰），信息差运转正常；卷二开头它该有名字（陆九），届时 entities upsert 改名。
6. 压惊钱 ±1 净额零入账：账本"事实在册"与"净额"分离，演示成立。
7. 称谓进度条（常翁）ch_006 堂前戏未落地——人物卡承诺要回写进 beats 的"线动作"栏，Stage 1 模板加一行自查（工程待办）。
8. 本卷 GUN 结算：1 兑现 1 顺延 1 新开；MIS 全清。章后修订无旁路：每章仅一个在途提案（{ch}.json），
   GUN-002 顺延更新并入 ch_007 提案（sync 时一并合并）——CLI 对 ch_006.patch-a 报'无提案'而非格式错，提示语可改进（M6 候选）。
9. 下一步 ch_007：盐行暗账线开卷二（"平"册禁手），pack 应命中 誊/平/蓝布 相关实体——若别名失配先补 aliases。
10. 盲测结论沿用：流程差异>文笔差异。卷二用同一套 craft，基线章不再仿写。

## ch_007 收章记（卷二开局，v1.2/v1.3 新规首航）

- 两幕合同首跑成立：润色幕实改 4 处（物感、对话、收束、视角泄漏），校对幕抓出润色稿
  自埋违例 2 处（"无数遍"混入、直引号 24 个未转）——两幕分权的设计价值当场兑现。
- **新规实录**：初稿直接 1233 字偏短老病复发，但扩写依据从"猜"变成 evidence file 实数；
  章带 1800-2600 与 ch_006（2400 下沿）错开 600——spread 从 19 拉到 364，方差条生效。
- 视角首现名教训：叙述者先叫"陆九"等于替沈拓起念——人物得名必须经人物口；
  已回写本卷每章校对义务（记于 ch_007 注记「打磨」节 4）。
- proposal new 骨架 + sync 一次通过（零拒收）；GUN-002 顺延 update 按卷一预案落地；
  GUN-004（外字捌'借'签）plant，target ch_009。
